import type { IResourceItem } from '@refinedev/core';

const siteSettings: IResourceItem = {
  name: 'site-settings',
  list: '/admin/site-settings',
  meta: {
    label: 'Thiết lập trang web',
    icon: 'GlobalOutlined',
  },
};

export default siteSettings; 